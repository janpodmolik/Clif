import DeviceActivity
import SwiftUI

extension DeviceActivity.DeviceActivityReport.Context {
    static let totalActivity = Self("Total Activity")
    static let onboardingOverview = Self("Onboarding Overview")
}
